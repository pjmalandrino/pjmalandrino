[package]
name = "llvq-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Exact arithmetic core for Leech Lattice Vector Quantization (arXiv:2603.11021): extended binary Golay code and integer-coordinate Leech lattice."

[dependencies]
# Intentionally empty: the mathematical core must stay auditable and
# reproducible with zero external dependencies (gate G1 requirement).
